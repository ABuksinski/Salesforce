public  class AutoNumbering {
//     public static void  AutoNumbering(List<Course_Invoice__c> listOfInvoices) {
//  //Fetch record types for the Custom Object - 'Course_Invoice__c'
//  map<Id,String> mapOfRecordTypeIdVsName = new map<Id,String>();
//  list<RecordType> recordTypeList = [Select Id, Number__c from RecordType where sObjectType = 'Course_Invoice__c'];
//  if(recordTypeList != null && !recordTypeList.isEmpty()){
//      for(RecordType rec : recordTypeList){
//          mapOfRecordTypeIdVsName.put(rec.Id,rec.Number__c);
//      } 
//  }
 
//  //Fetch the Invoice Number of the last created Invoice
//  list<Course_Invoice__c> invoiceList = new list<Course_Invoice__c>();
//  invoiceList = [SELECT Id, Number__c  FROM Course_Invoice__c Order by CreatedDate  desc limit 1];
//  system.debug('invoiceList*****'+invoiceList);
//  decimal autoNumber;
 
//  if(invoiceList != null && !invoiceList.isEmpty() && invoiceList[0].Number__c != null && invoiceList[0].Number__c != ''){
//      String autoNumberString = invoiceList[0].Number__c.substringBetween('{','}');
//      autoNumberString = autoNumberString.trim();
//      autoNumber = Decimal.valueOf(autoNumberString);
//  }else{
//      autoNumber = 000000;
//  }
 
//  //Update Invoice Number
//  for(Course_Invoice__c inv : listOfInvoices){
//      String recordtype = null;
//      if(mapOfRecordTypeIdVsName != null && inv.RecordTypeId != null && mapOfRecordTypeIdVsName.containsKey(inv.RecordTypeId)){
//          recordtype = mapOfRecordTypeIdVsName.get(inv.RecordTypeId).left(3);
//      }
     
//      if(recordtype != null && recordtype != '' && autoNumber != null){
//          String autogenerated = String.valueOf(Integer.valueOf(autoNumber) + 1);
//          autogenerated = autogenerated.leftPad(6,'0');
//          inv.Number__c = recordtype+'-{'+autogenerated+'}';
//      }
//      autoNumber++;
//  } 
    
// }
}